version: 2

models:
  - name: stg_adw__headersalesreason
    description: staging da tabela salesreason
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - order_fk
            - reason_fk
  

  